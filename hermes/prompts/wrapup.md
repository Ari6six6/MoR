Only 2 turns remain in this run before the hard cap. Stop opening new work.
Use the next turn for the single most important remaining step if it is
quick, then call `finish_run`. Your summary must list precise open items
(file paths, commands, next step) so your next run can continue exactly
where this one stopped.
